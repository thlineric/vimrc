" =====================================================
" vimrc
" -----------------------------------------------------
" Ting-Hung <Erik> Lin
" Fork me on Github https://github.com/thlineric/
"
" Keep It Simple, Stupid - VIM
"
" 2018/06/24
" =====================================================

" the following is default setting for vim init-sequence
" user can override the g:env_init_sequence to adjust init-sequence
" let g:env_init_sequence = [
"     \'plugins' ,
"     \'basic'   ,
"     \'config'  ,
"     \'style'   ,
"     \'keymaps' ,
" \]

" user can change the path of bundle folder
" let g:bundle = '~/.vim/bundles'

" adjust the table to enabled/disabled the bundle_group
" about the bundle_group please refer to env/plugins.vim for detail
" let g:bundle_group =  {
"     \ 'auto_complete'  : 1,
"     \ 'basic'          : 1,
"     \ 'hl_syntax'      : 1,
"     \ 'program'        : 1,
"     \ 'qf_enhanced'    : 1,
"     \ 'search'         : 1,
"     \ 'source_control' : 0,
"     \ 'textobj'        : 0,
"     \ 'ui'             : 1,
" \ }

" the variable will be ignored when you have not installed YankRing
" let g:copy_cache_dir = expand('~/.vim/cache')
" let g:copy_cache_dir = expand(g:core . '/cache')

" the variable will be ignored when you have not installed gutentags
" let g:tags_cache_dir = expand('~/.vim/cache')
" let g:tags_cache_dir = expand(g:core . '/cache')

" the variable will be ignored when you have not installed leaderf
" let g:leaderf_cache_dir = expand('~/.vim/cache')
" let g:leaderf_cache_dir = expand(g:core . '/cache')

so C:/github/vimrc/core.vim
